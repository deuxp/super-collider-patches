s.boot

// sig = { [SinOsc.ar(440, 0, 0.2), SinOsc.ar(482, 0, 0.2)] }.play;
// sig.play;
//sig.free;

f = { "Func eval".postln };
f;

(
"ypur the ned".postln;
"fuck righ toff".postln;
"my name is md".postln;
)

s.quit